{%- if video != blank -%}
  {% render 'video-with-alt',
    video: video,
    class: 'images-with-text__image-list-item-media',
    autoplay: true,
    loop: true,
    muted: true,
    controls: false,
    loading: 'lazy', 
    iterate_on_click: true
  %}
{%- elsif image != blank -%}
  <img
    {% render 'image-attributes', image: image, class: 'images-with-text__image-list-item-media' %}
  >
{%- endif -%}
